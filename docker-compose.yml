services:
  relayer:
    build:
      context: .
      dockerfile: Dockerfile
      target: relayer
    ports:
      - 3111:3111
      # - 3112:3112
    environment:
      - PORT=3111
    restart: always
